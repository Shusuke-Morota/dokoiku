<% if user_signed_in? && @user != current_user %>
<% if current_user.following?(user) %>
<%= link_to "フォロー中", unfollow_path(user.id), method: :POST, class: "btn btn-primary", remote: true %>
<% else %>
<%= link_to "フォローする", follow_path(user.id), method: :POST, class: "btn btn-outline-primary", remote: true %>
<% end %>
<% end %>